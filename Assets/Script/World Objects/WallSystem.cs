using UnityEngine;

namespace Script.World_Objects
{
    public class WallSystem : MonoBehaviour
    {
        public GameObject ExampleWall;

        public GameObject root;

        // Start is called before the first frame update
        void Start()
        {
        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}